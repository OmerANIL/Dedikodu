# Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/dedikodu?schema=public"

# JWT Auth
JWT_SECRET="super-secret-key-change-me-in-production"
JWT_EXPIRATION="7d"

# SMTP Settings for NodeMailer (Gmail etc.)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"
